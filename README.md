# 도살장 격전지 계산기 V1.5.0

## V1.5.0 UI 개편 (2026-09-23)
- 기존 계산식, 길드원 격추 데이터, 검증/결과 출력 로직을 유지하고 입력 UI만 선택형으로 개편했습니다.
- 사용법 박스를 제거하고 `1구역 / 2구역 / 개인 초대형 / 개인 종합 격추` 선택 후 닉네임 중심으로 입력합니다.
- 2구역은 루이·루니·루나·바나냥, 개인 초대형은 늑대·해적·고양이·음식 4등분 버튼을 사용합니다.
- 길드원 격추표는 첫 화면 하단에 상시 표시합니다.
- `길드 초대형 계산`은 페이지 이동 없이 같은 페이지 안에서 펼쳐집니다.
- 계산 종류 선택창과 닉네임 입력창은 모바일에서 과도하게 두껍지 않도록 동일 높이 48px의 컴팩트 입력부로 조정했습니다.


GitHub Pages 전체 업로드 파일 / 생성일 2026-09-22

## 업로드
다음 4개 파일을 저장소 루트에 업로드합니다. index.html은 반드시 루트에 둡니다.
- index.html
- README.md
- dosaljang-preview-20260917-v120-verified.png
- dosaljang-profile-v123.jpg

이미지 파일명 속 날짜는 기존 자산 식별자이며 생성일이 아닙니다.

## V1.5.0 화면 개선
- 상단 탭 제거. 기존 계산기가 첫 화면입니다. 계산하기·검증 아래, 초기화·길드원 격추표 위에 전체 너비 짙은 로즈색 진입 버튼을 배치합니다.
- 지역·서버 버튼 34px, 시간 입력창 44×30px(모바일). 입력창 안내 글자 제거.
- 합계 박스 위치 상단 이동, 6열 표 및 최소점수 표시. 지역 헤더는 선택 전 지역, 선택 후 늑대·해적·고양이·음식으로 변경됩니다.
- V1.4.0의 계산식·길드원 데이터 그대로 유지.

## 초대형 스코어 계산
- 기존 계산기가 접속·새로고침 시 항상 첫 화면입니다.
- `균열(은하) 초대형 계산하기` 버튼으로 진입하고 초대형 화면의 `계산기로 돌아가기` 버튼으로 복귀합니다. 양쪽 입력값·계산 결과를 유지하며 돌아오면 기존 스크롤 위치를 복원합니다.
- 초대형 지역과 서버격추는 처음에 미선택입니다. 서버 0%는 정상적인 선택값입니다.
- 기존 계산기와 새 탭의 서버 선택은 독립적이며, 15명 데이터 원본은 동일하게 사용합니다.
- 표: 닉네임 / 입력(초) / 기본 / 지역 / 최소점수 / 예상스코어. 지역 헤더와 각 길드원의 지역 수치가 선택에 따라 함께 변경됩니다.
- 시간은 0~1,200초 정수입니다. 빈칸·0은 참여 제외입니다. 잘못된 입력이 있으면 총합계를 표시하지 않습니다.
- 개인 예상 스코어 = 반올림(1,050 × 사용 초 × (1 + (기본 + 서버 + 지역)/100) + 개인 최소 점수).
- 최소 점수는 시간이 양수인 참여자에게 이번 보스 1회분만 가산합니다. 길드 합계는 화면에 표시된 개인 예상 스코어들의 합입니다.
- 기존 63 WAVE/60초 모델을 평균 시간당 점수로 환산한 추정입니다. 초대형 전용 실측 보정이나 순위·포인트 예측은 포함하지 않습니다.
- 사용 시간 초기화는 새 탭의 시간만 지우며 지역·서버 선택은 유지합니다.
- 예상 합계 스코어는 서버격추 선택 아래, 표 위의 별도 박스로 표시합니다. 시간/지역/서버를 변경하면 자동 갱신됩니다.
- 지역 선택은 요청한 4개 지역만 지원합니다. 은하 중앙 지역은 데이터·규칙 확정 전 포함하지 않습니다.

## V1.4.0 검증
- 기존 계산 동작 84개 사례 및 15명 전체 데이터 확인.
- 새 계산 4지역 × 6서버 × 15명 = 360개 사례 확인.
- 입력 경계, 미선택/0% 구분, 미참여 최소점수 제외, 총합계, 탭 입력 유지, 초기화 확인.
- 기존 주 계산 스크립트는 버전 표기 외 동일합니다.
- 실제 모바일 브라우저 렌더링은 이 환경에서 확인하지 못했습니다.

## V1.3.1에서 유지한 사항
- 승부왕 기본격추(탈것 포함): 53.2%. 최소 점수 1,000 및 지역격추 유지.
- 토글: 기본 input 테두리·여백 영향을 제거하고 손잡이 수직 중앙정렬, ON/OFF 동일 간격 적용.
- 돗돔 기본격추(탈것 포함): 185.3%.
- 입력 안내: “사용법에 따라 원하는 계산을 입력하세요.” / 글자색 #70558F.
- 기능명: “개별 전투시간 설정”. 기본 OFF, 직접 ON한 경우에만 시간 입력을 해석합니다.
- 사용법 제목: “[개별 사용 시간 설정 사용법]”. 제목 색상 #7138B8.
- 설명: “닉네임 뒤에 사용 시간을 초 단위로 입력하세요.”
- 조건 안내: “최소 1명은 시간 없이 닉네임만 입력하세요.”
- 예시: “예) 루나 / 돗돔 옥돔40 승부30”. 일반 사용법과 동일한 예시 태그 디자인을 사용합니다.
- 지역 안내: “늑대, 해적, 고양이, 음식 지역” / “[상층] 1구역, 2구역, 3구역 지역”.
- 길드원 현황 제목 박스: 연한 라벤더 배경·연보라 테두리·짙은 바이올렛 제목·차분한 보라색 기준일.
- 상단 제목: Pretendard Medium 500 우선, #7138B8, 모바일 22px. 날짜·버전은 600으로 조정.
- Pretendard는 고정 버전 1.3.9 CDN 웹폰트를 사용합니다. 로딩 실패 시 Apple SD Gothic Neo / Noto Sans KR / sans-serif 순으로 대체됩니다.
- 존재하지 않는 manifest.webmanifest 참조를 제거했습니다.

- 입력 안내 글자 크기: 14px.
- 버튼: 계산하기 / 검증, 초기화 / 길드원 격추표의 2×2 동일 크기 배치.
- 계산하기·길드원 격추표는 기존 primary 디자인, 검증·초기화는 기존 secondary 디자인. 검증 및 초기화 기능 유지.

## 유지하는 UI 규칙
- 작은 원형 이미지 + 제목 한 줄, 그 아래 날짜·버전. 날짜는 제목 기준 중앙정렬.
- 상단 여백 및 입력창과 기능 스위치 사이 축소된 간격 유지.
- “계산 전 현재 서버격추를 선택하세요.”를 숫자 선택 버튼 위에 표시. 앞 기호 없음.
- Royal Violet B 결과·공유·레벨·길드원 표, 모든 표 셀 중앙정렬.
- 일반표 닉네임 열 경계: 헤더 #d8c8f5 / 본문 #e5dcf6, 1px 실선.
- 상층 6열은 한 화면, 닉네임 sticky 없음.
- 길드원 상세표는 첫 진입·새로고침 시 숨김. “길드원 격추표” 버튼으로 펼침/접기, 기본 탭 일반 | 균열 | 은하.
- 최소 점수 설명 문구는 기존 확정 문구 유지.
- 개별 전투시간 대상자의 이탈 기준은 입력한 시간(30초 등)으로 표시. 공유 결과도 동일.
- 일반 결과 제목에는 “예상시간”을 붙이지 않으며, 일반 이탈 기준은 73.xx%와 같은 형식.

## 최신 기본격추 / 최소 점수
돗돔 185.3 / 매콤갈비 142.5 / 사마엘 95.5 / 혀녕 83.8 / XIGN 78 / 뱅수띠 64 / 토시살 57 / 페르시안 57 / 진니 51.5 / 승부왕 53.2 / 데이다라 40 / 바포메트 35.5 / 옥돔 34.5 / 하니엘 29 / 벨제붑 21.

최소 점수: 돗돔 4,500 / 혀녕 1,500 / 승부왕 1,000 / 나머지 0.
개인 데이터에 서버격추를 저장하지 않습니다. 등록 별칭 및 지역격추는 최신 기준본을 유지합니다.

## 계산 규칙
- 서버격추 선택값: 0 / 1 / 2 / 3 / 4 / 6%. 첫 진입 미선택. 0과 미선택은 다릅니다. 자동 초기화 없음.
- 종합격추 G = 기본격추(탈것 포함) + 선택 서버격추 + 해당 지역격추.
- 화력 P = 100 + G. 분배는 P / 참여자 P 합계.
- 실전 목표 합계 100, 1구역 정수 담당 HP 합계 입력 HP. largest remainder, 소수점 동률은 G 우선.
- 최소 점수 M은 최종 점수에 전투당 한 번 가산: Score = B × (1 + G/100) + M. 피해량·HP 분배·완료시간에는 더하지 않습니다.
- 시간 모델 유지: WAVES_PER_SEC = 63/60, SCORE_PER_WAVE = 1000, SAFE_SEC = 1.
- 개별 전투시간 설정은 모두 동시에 입장하는 조건. 각 대상자의 시간 × 기존 실전 화력으로 피해량을 계산하고, 남은 HP를 정상 참여자의 화력비로 분배합니다.
- 최대레벨 HP: 루이 Lv20 350,000 / 루니 Lv30 400,000 / 루나 Lv40 450,000 / 바나냥 Lv50 500,000.
- 시간 입력: 옥돔30초 / 옥돔 30초 / 옥돔30 / 옥돔 30. 복수 대상자·혼합 입력 가능.
- 결과 하단은 선택 서버격추와 종합격추 공식 두 줄만 표시합니다.

## 검증 범위
- 직전 최신 V1.3.1 전체 ZIP과 비교: 승부왕 기본격추·기준일, 제목 서체·색상 및 토글 정렬 변경. 계산식·나머지 데이터·이미지 유지.
- 15명 기본격추·최소 점수·상층 및 일반 지역격추 대조, 별칭 및 폐기 닉네임 거부 확인.
- 84개 계산 사례, 서버 미선택 차단·6개 선택값, 일반·1구역·2구역·단독 조회, OFF 차단, 4가지 시간 표기·복수 대상자, 검증·초기화·공유표 실행 확인.
- 정수 HP/목표 합계, JS 문법, ZIP CRC·재해제 파일 일치 확인.
- DOM 대체 객체를 통한 실행 검증이며 브라우저 시각 렌더링·실제 모바일 화면 및 외부 웹폰트 다운로드는 이번 환경에서 확인하지 못했습니다.

## V1.5.0 추가 미관 개선
- 초대형 진입 버튼: 로즈 #9D365B, 테두리 #7F2848, 흰색 글자. 위치·크기 유지.
- 합계 박스: 첫 줄 제목/참여 인원, 둘째 줄 점수. 모바일 기준 내용과 여백 합계 약 68px로 축소.
- 계산식과 회원 데이터는 변경하지 않았습니다.

## V1.5.0 돌아가기·표 정돈
- 돌아가기 버튼: 로즈색/흰색 글자, 오른쪽 정렬, 가로 1/3(문구를 위한 최소 너비 140px), 높이 42px 유지.
- 계산 완료 내부 검증 문구에서 보정 날짜만 제거. 상단 날짜와 과거 실측 기록은 유지.
- 초대형 표 헤더·본문 전체에 연한 라벤더 1px 가로·세로 실선 적용.

## V1.5.0 최종 5열 정돈
- 돌아가기 버튼 크기·색상 유지, 중앙 정렬 및 위 12px/아래 20px 여백.
- 초대형 표에서 최소점수 표시 열만 제거. 계산에는 기존 최소점수를 계속 반영.
- 닉네임/입력(초)/기본/지역/예상스코어 5열. 지역 헤더는 선택한 지역명으로 변경 유지.
- 초 입력 글꼴을 본문과 통일하고 13px·중간 굵기로 조정(넓은 화면 14px). 입력칸 크기 유지.

## 2026년 9월 23일 생성본 · V1.5.0
- 파일 생성일 기준으로 화면·공유 날짜 갱신. 과거 보정 기록 날짜는 보존.
- 격전지 추가 점수 아래 종합/기본/지역 2단 헤더. 종합은 기본+서버+지역.
- 지역 헤더 동적 변경 유지, 미선택 종합은 — 표시.
- 6열 너비 18/14/17/16/11/24%, 행 높이·입력칸 여백 축소.

## 2026년 9월 23일 표 정렬 조정
- 종합/기본/지역 열을 각각 16%로 통일, 예상스코어 20%. 닉네임 18%·입력 14% 유지.
- 헤더는 중앙 정렬. 종합/기본/지역/예상스코어 본문만 오른쪽 정렬, 오른쪽 여백 5px.
- 계산 코드·데이터·지역 헤더 동작 및 다른 디자인 유지.

## 2026년 9월 23일 화면 배치·강조 조정
- 일반 계산기: 사용법 → 서버 선택 → 입력창/시간 토글 → 기존 버튼 순서.
- 복귀 문구를 일반 계산기로 돌아가기로 변경, 중앙 배치 유지.
- 종합 열 외곽만 2px 바이올렛 강조, 닉네임 굵게.
- 초 입력칸과 숫자 소폭 축소. 계산 코드와 데이터는 직전 파일과 동일.

## 종합 헤더 윗선 수정
- 병합 헤더의 border-collapse 경계 공유로 선이 연장될 수 있어, 종합 헤더의 윗선만 셀 내부 inset 그림자로 교체. 기본·지역 경계는 기존 1px.
- 다른 디자인, 데이터 및 스크립트는 동일. 실제 iPhone 렌더링은 별도 확인 필요.

## 최종 툴바 및 종합 외곽선 수정
- 간단한 두 줄 안내 왼쪽, 사용 시간 초기화 버튼 오른쪽의 한 줄 툴바.
- 표 아래 예상값 안내는 지정한 문구와 명시적 줄바꿈 적용.
- 종합 헤더부터 마지막 수치까지 실제 셀 좌표 기준의 단일 사각형 외곽선. 표 크기·화면 전환 시 재배치.

## 하단 안내·여백 정리
- 표 아래 자동 계산 문구와 예상값 안내 삭제, 마지막 행 아래 카드 여백 10px.
- 표 위 두 줄 안내를 지정 문구 및 진한 파란색 #174B86으로 변경.
- 잘못된 시간 입력 오류는 표 위에만 표시하며 정상 상태에서는 공간을 차지하지 않음.
- 계산식·데이터 및 종합 테두리 좌표 코드는 유지.
