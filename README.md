# 도살장 격전지 계산기 V 1.2.3

GitHub Pages 전체 업로드 파일 / 2026-09-17 개편본

## V1.2.3 업데이트 핵심

- 서버격추를 길드원 고정 데이터에서 분리
- 계산 전 현재 서버격추를 `0% / 1% / 2% / 3% / 4% / 6%` 중 사용자가 직접 선택
- 서버격추 미선택 상태에서는 계산하지 않도록 차단 (`0%`도 직접 선택해야 함)
- 서버격추 선택 변경 시 이전 계산 결과를 그대로 사용하지 않도록 처리
- 종합격추 계산식은 `기본격추(탈것 포함) + 선택 서버격추 + 지역격추` 유지
- 계산 결과에 `이번 계산에 선택된 서버격추는 X%입니다.` 표시
- 길드원 격추 현황표에서 서버격추 열 삭제
- 현황표에서 `지역격추 → 종합격추` 형태의 사전 계산값을 제거하고 원본 지역격추만 표시
- 상층 현황표: `닉네임 / 기본 / 1구역 지역 / 2구역 지역`
- 일반·균열 현황표: `닉네임 / 기본 / 늑대 / 해적 / 고양이 / 음식`
- 모바일 좌우 스크롤 시 닉네임 열 고정
- `닉네임만 입력 : 지역별 본인의 종합격추 확인` 기능 추가
- 닉네임 단독 입력 시 선택한 서버격추를 반영하여 상층 1·2구역 및 일반·균열 각 지역의 종합격추 표시
- 기존 `닉네임만 입력 → 2구역 자동 분배` 동작 폐기
- 1구역 체력 분배, 2구역 보스 분배/이탈 기준, 일반·균열 계산 및 기존 실측 보정 구조 유지

## 현재 관리 기준

- 버전: V 1.2.3
- 최신 반영일: 2026-09-17
- 길드원: 15명
- 돗돔 기본격추: 183.9%
- 서버격추는 저장된 현재값으로 관리하지 않으며 계산 시 사용자가 직접 선택

## GitHub Pages 업로드

압축을 푼 뒤 아래 **4개 파일을 저장소 최상단(root)** 에 업로드합니다.

- `index.html`
- `README.md`
- `dosaljang-profile-v123.jpg`
- `dosaljang-preview-20260917-v120-verified.png`

기존 `index.html`과 `README.md`가 있다면 새 파일로 교체되어야 합니다.


## V1.2.3 표 표시 보정
- 길드원 격추표 닉네임 열 좌우 스크롤 고정
- 닉네임/기본/지역 열 폭을 원본 수치에 맞게 축소
- 지역격추 0은 `-`가 아니라 `0%`로 표시


## V1.2.3 표 완성도 수정
- 상층 안내: `[상층] 1구역, 2구역 지역 격추`
- 일반·균열 안내: `[일반(균열)] 늑대, 해적, 고양이, 음식 지역 격추`
- 닉네임 열 좌측 고정 유지
- 상층 기본/1구역/2구역 동일 폭, 숫자 우측 정렬
- 일반·균열 숫자 열 폭 재분배 및 우측 정렬
- 지역 0값은 `0%`로 표시
- 표 외곽 오른쪽 선 포함 전체 테두리 보정


## V1.2.3 UI 마감 수정
- 계산 전 서버격추 안내 아래 `서버격추는 트로피 아래 「서버 게시판」을 통해 확인할 수 있습니다.` 추가
- 상단 및 공유 화면 길드 프로필 이미지를 새 도살장 붉은 달·늑대 이미지로 교체
- 상층/일반·균열 닉네임 글자 크기·굵기 완전 통일
- 닉네임 열 좌측 고정 유지 및 고정 영역 경계선 강화
- 상층 `닉네임 / 기본 / 1구역 지역 / 2구역 지역` 4열 동일 폭
- 일반·균열 숫자 열 폭 재조정
- 모든 데이터 열 사이 세로 구분선 추가, 숫자 데이터 우측 정렬
- 지역격추 0은 모든 현황/단독조회에서 `0%`로 표시


## 2026-09-17 데이터/UI 최신 반영 (V1.2.3 유지)
- 버전 번호는 V 1.2.3 그대로 유지
- 돗돔 기본격추(탈것 포함): 183.9%
- 승부왕 지역격추: 상층 1구역 0%, 상층 2구역 0%, 늑대 0%, 해적 +1%, 고양이 0%, 음식 +1%
- 계산 입력 하단 사용법을 ①~④ 구조로 재정돈하고 입력 형식의 색상 대비 강화
- 일반(균열) 예시 `음식 / 토시살`을 별도 강조색으로 표시
- 사용법 하단에 `길드원 종합격추표는 페이지 최하단에서 확인할 수 있습니다.` 안내 추가
- 2026-09-17 루나 Lv.37 / 돗돔 단독 / 서버격추 1% / 당시 종합격추 179.9% / 실제 148초 실측을 차후 보정용으로 보존
- 위 실측은 당시 기본 173.9% 기록으로 보존하며 현재 183.9%로 소급 변경하지 않음

## 2026-09-17 최종 화면 조정
- 서버 안내 첫 줄: `※ 계산 전 현재 서버격추를 선택하세요.`
- 위 첫 줄은 과도하게 굵지 않은 중간 강조의 빨간색으로 표시
- 서버 게시판 확인 안내는 기존 회청색 유지
- 모바일 사용법 ①~④의 항목 간격과 제목-설명 간격을 축소해 계산 버튼이 과도하게 아래로 밀리지 않도록 조정
- 사용법 순서·문구·예시 배지·최하단 격추표 안내 구조는 유지
